# Визуализация кластеризации (KMeans, DBSCAN)

- Клик мышью (левая кнопка) — создаёт группу случайных точек
- Нажатие `K` — запускает кластеризацию KMeans
- Нажатие `D` — запускает кластеризацию DBSCAN
